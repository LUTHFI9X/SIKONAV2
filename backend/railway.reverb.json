{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "watchPatterns": [
      "/backend/**"
    ]
  },
  "deploy": {
    "startCommand": "sh -lc 'PORT_VALUE=$(printf \"%s\" \"$PORT\" | tr -cd \"0-9\"); [ -n \"$PORT_VALUE\" ] || PORT_VALUE=8080; php artisan optimize:clear || true; php artisan reverb:start --host=0.0.0.0 --port=$PORT_VALUE'",
    "healthcheckPath": "/up",
    "healthcheckTimeout": 300,
    "numReplicas": 1,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
